# video-txt 开发记录

这里放的是做技术决定时的实测数据和取舍过程,不是使用说明——怎么用看 [README.md](README.md)。
留档是为了以后改动时不用重新踩一遍坑。

## 配音为什么按句子合成

字幕是按阅读节奏切的,一句话常被切成三四条。早期版本逐条字幕合成,结果是两个问题叠在一起。

**语调不连贯。** 逐条合成时每条都带句末降调和收尾停顿,一句话拆三条读出来就是三次"陈述完毕"。

**凭空多出大量时长。** 同样的文字,逐条合成比整句合成平均每条多花约 0.5 秒(各自的起收和句末停顿)。
604 条字幕就是 5 分多钟——而当初逼着我们各种提速的时长缺口一共也就 5.6 分钟,几乎全是切碎自己造成的。

改成先把连续字幕拼回完整句子、整句一次读完、再放回这句第一条字幕的时间点。拼句规则:
遇到 `。！？…` 收尾、或停顿超过 1.5 秒、或已攒到 120 字就断开;开了 `--diarize` 时换人一定断开。
本来每条就是完整句子的短片不受影响。

另外每段 TTS 语音开头有约 0.17 秒固定静音,现在自动裁掉,人声正好落在字幕时间点上,不再整体慢半拍。

旧行为保留在 `--voice-unit line`,只用于对照。

## 长视频实测

一条 24 分钟英文演讲、604 条字幕。同一份字幕、同一个音色,只改合成粒度:

| 配置 | 语音段数 | 被拉伸的段 | 最大漂移 |
| --- | --- | --- | --- |
| `--voice-unit line`(旧行为) | 604 | 489 | **75.3s** |
| 默认(按句子) | 181 | 120 | **3.4s** |
| 默认 + `--rate +10%` | 181 | 84 | **1.3s** |

604 条字幕其实只有 181 句话。

**缺口优先用 `--rate` 补,不要用 `--max-atempo`。** 第三行只加了 `--rate +10%`,被拉伸的段就从 120 降到 84,
漂移还更小。TTS 用更快语速重新说一遍是自然的,ffmpeg 把录好的波形拉伸不是。

**按句子合成之后不用再改译文。** 之前这条片子得靠 `--fit-duration` 改写 293 句、外加 +25% 语速和 2 倍拉伸,
才把漂移压到 8.6 秒;现在什么都不加就是 3.4 秒。那种把"或搜索时使用"砍成"搜索时"的电报体可以不要了。

重新合成 181 段约 3 分半(旧的 604 段慢得多)。改 `--rate` 或 `--voice` 会让语音缓存整体失效,别反复试。

## 超时句子:先重说,再拉伸

上一节"缺口优先用 `--rate` 补"的结论现在自动化到了每一句:合成后量一遍时长,塞不进自己格子的
句子按需要的倍数换更快的语速**重新合成**,`atempo` 只兜重说仍不够的残余。TTS 用更快语速再说
一遍是自然的快语速,拉伸录好的波形才是机械感的来源——所以拉伸永远是最后手段。

几个参数的道理:

- **1.05 倍以内不重说。** 这么小的拉伸听不出来,多合成一段什么也买不到。
- **语速按 5% 一档向上取整。** 向上,因为差一头发丝就还得拉伸;取整,是让重跑落在同样的档位上、
  命中同样的缓存(缓存键含语速)。
- **封顶 1.6 倍。** 再快就不像说话了,剩下的交给 `--max-atempo`。真到这一步的句子本来就塞不下,
  比如 2 秒格子里 36 个字,怎么处理都会超,报告里的 `Still longer` 抓的就是这种。
- **基础语速会乘进去。** `--rate +10%` 之下还差 1.2 倍的句子,重说档位是 1.1 × 1.2 ≈ +35%。
- edge-tts 按 rate 重说;克隆引擎每个 job 自带 speed 倍率(IndexTTS 换算成
  `duration_factor` 的倒数,是模型原生的时长控制);只有 `say` 照旧走拉伸。
- **克隆引擎的重说必须一次进程搞定。** 模型加载几十秒,按档位分批就是按档位反复加载;
  所以超时句子不分批,每个 job 记自己的语速,一个 worker 全做完。edge-tts 顺带也改成了
  单批(它本来就是每句一条命令,分不分批无所谓)。
- 原速片段虽然被重说版替换,但**每次重跑都要先用它们量时长**,所以算"用到的",`--prune-cache` 不删。

## 背景声:压低整轨 vs 人声分离

`--keep-bgm` 是把整条原声压到 15% 混进去——原讲者还在底下小声说外语,跟中文配音打架;
不开的话背景是纯数字静音,音乐、掌声、环境声全没了。两头都不对。

`--separate-bgm` 用 Demucs(htdemucs,两分频)把人声抠掉,配音混在纯背景上,默认全音量:
分离出来的背景就是原片减去人声的那份混音,响度平衡是制作方调好的,不需要再压。
25 秒的测试片在 M 系 CPU 上分离约 20 秒,首次另加一次模型下载;结果转成 FLAC(比 WAV 省一半)
缓存在 `dub-cache/bgm/`,重跑直接复用,`--prune-cache` 不动它。

两分频本来就同时产出人声和背景两条轨,以前只留背景。人声轨(`bgm/vocals.flac`)现在也留着,
用来剪克隆参考片段:参考片段是要让模型学音色的,底下压着配乐等于让它连配乐一起学。
20 秒人声压音乐的样片上,参考片段里说话已经结束、只剩音乐的那 0.3 秒,从混音剪是 -38.0 dB,
从人声轨剪是 -85.8 dB;整段参考的语音电平没变(-21.6 dB / -21.8 dB)。
因此分离挪到了挑音色之前跑——同一次运行里剪参考就能用上,顺带把"源视频没有音轨"提前报出来。

## 响度归一化与咔哒声

- 混音输出统一过一遍 `loudnorm=I=-16:TP=-1.5:LRA=11`(EBU R128,在线内容的常用响度),
  不同引擎、不同音色的成片响度从此一致。loudnorm 内部在 192 kHz 工作,输出端要钉回 48 kHz,
  否则这个采样率会一路进到 AAC 编码器。
- 每段语音的首尾加 8 毫秒线性淡入淡出。裁起始静音是按电平阈值切的,可能切在波形中间,
  直接落在数字静音上就是一声咔哒;8 毫秒短到听不出是淡入。

## 配音的翻译是口语体

`dub` 的翻译请求多带一条规则:译文是念给耳朵听的,要自然口语、短句,数字和缩写按读法写。
`run`/`mux`/`translate` 出的字幕仍是给眼睛看的,不带这条。同一个视频先做过字幕再配音时
`.zh.srt` 直接复用,不会自动重翻——想要口语版加 `--retranslate`,已合成的语音按内容哈希缓存,
只有译文真变了的句子会重新合成。

## IndexTTS-2:表现力最强的克隆引擎

edge-tts 的天花板是新闻腔——干净,但一部剧念下来全程一个情绪。2026 年中的格局里,
配音场景的开源第一梯队是 IndexTTS-2/2.5(bilibili 出品):情感相似度公开测评第一,
3 秒参考音频零样本克隆,原生**时长控制**(`duration_factor`,直接告诉模型这句说多快,
不是合成完再压缩)。商用 API 里 MiniMax speech-2.8 音质更稳但按字符计费,
本地跑 IndexTTS 免费且和 `--diarize` 一配就是每个角色自己的声线,选了后者做推荐克隆引擎。

接入方式和 CosyVoice 一样是"自己的 checkout + 自己的 venv":它把 Python 锁在 3.11、
torch 锁在 2.8,永远进不了本项目的 3.12 环境。好在 `clone_worker.py` 本来就是
为这种模型设计的独立进程——manifest 进、音频出,worker 不 import 本包任何东西。
新增的便利是 `--clone-repo` 指过去就行:checkout 里的 `.venv/bin/python` 和
`checkpoints/` 都自动找到,不用再写 `--clone-python` 和 `--clone-model`。

Apple Silicon 上的坑:MPS 缺个别算子,worker 统一设 `PYTORCH_ENABLE_MPS_FALLBACK=1`
兜底;半精度参数两代拼法不同(2.5 是 `use_bf16`,2 是 `use_fp16`),worker 看构造签名
自适应,`lang`/`verbose`/`duration_factor` 同理——README 之外没有接口文档,签名就是文档。

实测(M5 Pro 48GB,48 分钟剧集,384 句):合成 RTF 约 2-4,整集含重说约 100 分钟——
和 edge-tts 的三分钟完全是两个量级,这是本地大模型的真实代价。另外两个数字:模型对同一
参考音频的说话人编码有缓存,所以 manifest 里的任务按参考分组排序(对话按行交错会让缓存
每行都失效);短句的固定开销占大头(1.5 秒的句子 RTF 11,7 秒的句子 RTF 2.6),
`--voice-unit sentence` 合并出的长句正好也是省时间的形状。

## 没有字的行不配音

真实字幕里总有几行一个字也没有:"..." 表示停顿,歌词两头挂着音符。edge-tts 对这种文本回的是
NoAudioReceived 错误,一整集的合成停在半路。这类行现在不进 TTS:字幕照常显示,那几秒留给背景声。
首个真实触发:48 分钟的剧集,英文台词 "..." 被忠实地译成"……",合成停在 270/375 句——
只有真片子才带这种行,合成音和演讲视频里都遇不到。

判断条件后来收紧过一次。原先只查有没有 `\w`,于是 Whisper 把关门声、呼吸声写成的单个 `x`、`0`
照样被念出来,在那一秒念一声"ex"比留白更糟。现在整行只剩一个词字符就不发声。
边界是特意留的:中日韩单字(`好`)本身就是一个词,倒数(`3, 2, 1`)有三个字符,`42` 有两个,
都照旧发声。

## `--fit-duration` 时长感知重译

把读不完的句子自动改短:先合成一遍量出这个音色的真实语速,再把超时的句子连同"这个空档够读几个字"的
预算发回模型改写,改完重新合成,默认最多两轮。按句子合成之后多数时候用不上了,因为它会改动译文。

改写砍的是冗余不是信息:

| 原译文 | 重译后 |
| --- | --- |
| 欢迎观看这个关于 Claude Code 的简短演示。 | 欢迎观看Claude Code演示。 |
| 我们将在大约两分钟内连接一个 MCP 服务器。 | 约两分钟内连接一个MCP服务器。 |
| Anthropic 以快速的节奏发布新模型,所以请保持文档打开。 | Anthropic 出模型快,保持文档打开。 |

实现上的几个决定:

- **字数预算按音节算,不按字符。** `Claude Code` 是 11 个字符但只有两拍。按字符算会让含英文的句子
  语速虚高到 6.1 字/秒(纯中文只有 4.4),预算跟着压掉近一半,结果把中文砍到不成句。
- **额度按整句分配,不是每条字幕死磕自己那一格。** 一条字幕分到的是它按音节占整句的比例,
  长的那条能从短的那条借时间,不会因为跟别人共用一句就被判超时。
- **量的就是最后要念的那段。** 早先测量按每条字幕合成、渲染按整句合成,两套片段的缓存键互不相交:
  整片等于白合成一遍(实测 6 条字幕合出 11 段、最后只用 1 段),而且逐条片段各带一次起始静音和句末
  停顿——那是合成整句时根本不会付的开销,于是一批本来塞得下的句子被判超时、白改一遍译文。现在测量
  先合句再合成,量出的秒数按音节比例摊回各条字幕;时长和额度用同一套权重分,渲染阶段整轨命中缓存。
- **只动真读不完的句子。** `--fit-tempo`(默认 1.15)是能接受的加速倍数,超过才改;差不到 10% 的
  留给变速处理,不值得多跑一次 API。`--fit-tempo` 不能超过 `--max-atempo`,否则改完还是塞不下。
- **单轮最多砍三分之一。** 砍太狠模型会吐电报体,宁可留一点加速也别把话说断。
- 改写结果写进 `你的视频.zh.fitted.srt`,原 `.zh.srt` 不动;`--soft-subtitle` 会自动挂改写后那一份。

## 缓存结构与体积

`你的视频.dub-cache/` 下:

- 每句语音一个 mp3,键是内容哈希(含引擎、音色、语速)。换 `--voice` 或 `--rate` 整体失效。
  **哈希里不含字幕序号**:早期版本含,结果是在译文中间插一句就把后面所有语音的键推移一格、
  整片重合成,同一句话在片中出现两次也要合两遍。去掉序号之后改一句只重合成一句;代价是同一批里
  的重复台词会指向同一个文件,合成前要按路径去重,否则两个线程同时往一个文件里写。
- `shortened.jsonl`:`--fit-duration` 的改写结果,键是「原句 + 目标字数」,**不含音色**——换音色只是
  改变算出来的目标字数,同一个预算下该怎么缩句不用再问一遍模型。
- `reference/`:克隆用的参考音频和它的原文 `.txt`。

失效的旧语音不自动删,是为了换回原音色时还能复用。代价是调参会越堆越多:上面那条 24 分钟的视频
调完各种参数攒了 2579 个 mp3,其中 1667 个再也用不上。`--prune-cache` 只删这一次没用到的语音片段,
`shortened.jsonl` 和 `reference/` 不动。

合成好的整条人声先落成 `你的视频.zh-dubbed.dub.wav`,混进视频后删掉——24 分钟的片子这个中间文件
134 MB,跑几次对比很容易攒出几百兆。`--keep-dub-audio` 可以留;用 `--audio-output` 自己指定路径的
本来就不会删。

说话人分离结果在 `你的视频.speakers.json`,`--rediarize` 或换 `--diarize-model` 会重算。

## 硬字幕:顶部布局 vs 黑罩

原片底部已有烧死字幕时,实测**顶部布局(`top`)比黑罩(`bottom-box`)好用**:黑罩要么盖不干净,
要么为了盖干净而整片常驻、连没字幕的画面也挡掉底部一大块。顶部唯一要留意的是幻灯片类画面,
标题通常也在顶部可能压在一起;讲话人镜头没这个问题。

确实要用黑罩时的两条经验:

- 要**彻底盖住**旧字幕,`--hard-subtitle-box-opacity` 得给到 `1.0`,默认的 0.68 半透明会透出旧字。
- 高度按旧字幕实际占比给足,480p 视频上试出来要 `--hard-subtitle-box-height 0.30`,默认 0.22 会
  露出旧字幕的上半截。

字号和边距的单位是视频原始分辨率下的像素,不传时按分辨率换算:字号约为画面高度的 4.5%,底边距约 5%
(1080p ≈ 49px / 54px,480p ≈ 22px / 24px)。

用默认的 PingFang SC 时 ffmpeg 会打印几行 `Error opening font: ...PingFangUI.ttc`。这是 libass 先试
系统保留的那个字体文件、失败后回退到同族的 `PingFang.ttc`,字体照常生效、成片正常。介意日志干净
就换 `Heiti SC` 或 `Hiragino Sans GB`,这两个零报错。

## 语言处理

**源语言不假设是英文。** 转写不传 `--language` 时 Whisper 自己检测,翻译请求里源语言字段默认写
`auto-detect` 由模型判断,`--mode` 默认 `transcribe` 保留原语言(Whisper 自带的 `translate` 是 X→英文,
跟译中文无关)。实测一段日语音频:检测到 Japanese,转写
`こんにちは 今日は ai エージェントの記憶について話します`,译文 `大家好，今天我们来谈谈 AI 智能体的记忆。`,
全程没动任何语言开关。注意 Whisper 只拿前 30 秒判断语种。

**目标语言不限于中文。** `--target-language` 收自然语言名字,`Japanese`、`日语`、`日本語` 等价。
内置对照表覆盖中(简/繁分开)、英、日、韩、法、德、西、葡、意、俄、阿、印地、印尼、越、泰,
决定文件名后缀和字幕轨的语言标签。表外语言照样能翻,文件名回落成 `.translated.srt`、标签写 `und`
(未定)而不是瞎猜一个,`--language-code` 可手动覆盖。配音要另外给对应语言的音色,
默认那个是中文的。切句用的 `。！？` 日文韩文同样适用,拉丁语系按空格拼接。

## 一次真实的语种误判

一部 109 分钟的日语电影(4K BDRip),不指定 `--language` 直接跑。片头 30 秒是音乐没有对白,
Whisper 拿这 30 秒判断语种,判成了英语,然后整片带着错误的语种解码:

- 1235 行里 728 行是纯英文,只有 507 行带日文假名。
- 开头 5 分钟是每 30 秒一句 `Thank you.`,对着静音的幻觉。
- **1:03:45 之后到全片结束,90 次 `Let's go.`,每 30 秒一条**——最后 45 分钟没有任何真实内容。

问题是这些错误全都"看起来正常":译成中文之后是通顺的句子,直到烧完 68 分钟的 4K 硬字幕、
播起来才发现后半段不对。加上 `--language ja` 和 `--condition_on_previous_text False` 重跑,
同一部片子出 1849 条字幕、1788 条带假名,最高重复只剩 11 次的语气词。

所以有了 `quality.py` 的自动体检,拿两份真字幕校准出来的阈值:

| 信号 | 判定 | 坏字幕 | 好字幕 |
| --- | --- | --- | --- |
| 连续重复同一句 | ≥ 4 条**且**跨度 ≥ 60 秒 | 53 条 / 27 分钟 | 无 |
| 指定语种的字符占比 | < 60% | 44% | 99% |
| 字幕覆盖时长 | 最后一条结束点 < 媒体时长 50%(媒体 ≥ 5 分钟才判) | 0.9% | 99.3% |

跨度这一条是关键:真人对白也会连着喊同一个词,但不会连喊一分钟。只看重复次数会把
"はい はい はい はい" 这种正常台词误判成空转。语种检查只在显式传了 `--language` 且该语种
用非拉丁文字时才做——拉丁字母的语言(要法语给了英语)这样查不出来,就不假装能查。

覆盖时长抓的不是 Whisper 的病,而是**没下载完的文件**:种子按块乱序落盘,文件占位是全尺寸,
但只有开头一段能解码,Whisper 拿到 25 秒"完整"音频正常转完毫无察觉。一半的阈值放得很宽——
对白到片尾会变稀,但不会整个后半段消失。此项需要 ffprobe 探测媒体时长,探测失败就跳过不拦。

## 本地模型翻译:模型名从服务器问

DeepSeek 涨价后加了 `--provider lmstudio`,指向本机 `http://localhost:1234/v1`。云端 provider
预设里能写死默认模型名,本地不行——服务器上有哪个模型、加载没加载,只有服务器自己知道,
所以不给默认值,而是在没传 `--model` 时去问一次。

**问的是 LM Studio 自己的 `/api/v0/models`,不是 OpenAI 兼容的 `/v1/models`。** 后者把**下载过的**
模型全列出来,嵌入模型混在里面,拿第一条经常是个根本不会翻译的 embedding。前者每条带
`state` 和 `type`,只挑 `state=loaded` 且 `type` 是 `llm`/`vlm` 的,选中的模型会打印出来。
`vlm` 必须算进去:实测本机加载的 `qwen/qwen3.5-35b-a3b` 就被标成 `vlm`,只认 `llm` 会说没模型。

**key 是可选的,不是空字符串糊过去。** provider 预设里带 `requires_key: False`,取 key 时走
`resolve_optional_key`;否则没设 `LMSTUDIO_API_KEY` 会被凭据检查直接拦下,而 localhost 上的模型
本来就不认这个 header。

## 本地推理模型必须关掉思考

第一次拿 37 分钟的演讲试 `--provider lmstudio` 直接失败:`Timed out while calling the translation
API`,三次重试全超时,9 批一批没成。查下来不是网络也不是并发,是模型在**想**。

单独发一条只有两句话的翻译请求,回来的 usage 是:

| | completion tokens | 其中 reasoning | 耗时 |
| --- | --- | --- | --- |
| 默认 | 4845 | **4807** | 68 秒 |
| `reasoning_effort: "none"` | 17 | 0 | 0.3 秒 |

翻两句字幕想了四千八百个 token。一批 3200 字符的字幕自然撞穿 180 秒超时,而且想完给出的译文
跟不想时没有区别——字幕翻译本来就没有值得推演的东西。**200 倍的差距,零质量损失。**

所以 `lmstudio` 预设里把 `reasoning_effort` 定成 `none`,只在这个参数不是 `auto` 时才写进请求体
(云端 provider 保持 `auto`,即完全不提这个字段,免得 DeepSeek 那边收到不认识的参数)。想让本地
模型思考就显式 `--reasoning-effort high`。顺带补了 `--timeout`:本地慢,180 秒不够时能自己加。

试过但没用的写法:`chat_template_kwargs: {"enable_thinking": false}` 被 LM Studio 忽略(usage
一个 token 不差);prompt 里加 `/no_think` 只把思考从 4807 压到 941,不彻底。

## 本地模型答不完长列表:批次要小,失败要立刻拆

关掉思考之后同一条 37 分钟演讲(698 条字幕)还是一路报 `Batch validation failed`。看快照里的
原始响应,错误只有一种形状:**少了末尾几条,从不多给、也从不给错 id**。一批 71 条,它返回 61 条,
JSON 还规规矩矩地闭合——不是被截断,是它自己觉得说完了。

| 每批字符数 | 60 条样本的失败次数 | 耗时 |
| --- | --- | --- |
| 3200(云端默认) | 满屏失败,靠二分硬磨,11 分钟才过 2/9 批 | — |
| 1600 | 0 | 134 秒 |
| 800 | 0 | 99 秒 |

所以 `lmstudio` 预设把 `batch_chars` 定成 800。批次小了并不慢,反而更快:批多了并发填得满,
而且不用为一次失败付出重来的代价。

**更值钱的是另一半:格式错不再重试同样的请求。** 原来的逻辑是同一批重试 `--retries` 次
(默认 3 次)再对半拆。可温度是 0——同一个请求换来的是**同一个错答案**,快照里 attempt-01/02/03
少的是同一批 id,一个不差。现在 `ResponseFormatError` 只要还能拆就直接拆,重试留给网络和 HTTP 错误
(以及只剩一条、拆无可拆的时候)。同一条视频改完之后从每批约 40 秒降到约 9 秒。

**LM Studio 不认 `response_format: json_object`**,只收 `json_schema` 或 `text`,第一批会 400 并打印
`API rejected response_format; falling back to plain JSON prompting.`。已有的降级逻辑接住了:
同时在飞的那几个请求各撞一次(状态是收到 400 之后才置位的),所以 `--concurrency 4` 会看到 4 行,
之后的批次直接走纯提示词,不再重试。真要上 `json_schema`(每批按字幕 id 生成 schema、
强制输出合法 JSON)是另一件事,小模型上大概值得做,现在没做。

## 已有字幕审计:先保守识别,再安全修复

v0.3.1 把原来只会发现“连续重复/语种明显错误/转写过早结束”的体检扩成结构化审计。真实验收样本
是一份 1694 条英文电影字幕:`audit` 精确找出 8 条接近 30 秒、只有几个词的 Whisper 整窗幻觉,
以及 3 条零时长碎片。`clean` 删除前者、把后者并入紧邻前句并重新编号,得到 1683 条字幕;再次审计
已没有阻断级问题。

同一份字幕仍有 46 条显示超过 10 秒、58 条超过 42 显示列。这些并不一定错:对白可能本来就慢,
人工字幕也可能有意保留长行。因此它们只作为 warning 写进 JSON,不自动拆句。这个边界很重要:
时间轴重叠和倒序也只报告不猜修,因为没有词级时间戳时,擅自移动起止点比保留问题更危险。

## 多音轨选择:标题可以纠正错误的语言标签

真实双音轨电影给出了一个不能只信默认轨或 `language` tag 的反例:

| stream | 默认 | language | title | 实际内容 |
| --- | --- | --- | --- | --- |
| 1 | 是 | spa | Lat | 拉丁西语 |
| 2 | 否 | **spa(错)** | **Eng** | 英语 |

用户要求 `--language en` 时,标题 `Eng` 是最直接的正证据,因此 v0.4 选择 stream 2;如果没有语言要求,
默认标记与其他 disposition 信号差距不足,就停下让用户用 `--audio-stream` 决定。评分还会强降权
commentary、director、audio description 和 visual-impaired 音轨。第一、第二名差距小于安全阈值时
也不猜。

Whisper 的两个后端都没有稳定的“选容器音轨”接口,所以选择逻辑统一放在它们之前:ffprobe 读取
元数据,ffmpeg 用全局 stream index 提取 16 kHz 单声道 PCM WAV,OpenAI Whisper 与 MLX Whisper
只接收这份临时音频。这样两个后端和精修模式共享同一套行为,临时目录也能在异常时自动回收。

## 项目状态为什么按阶段保存指纹

v0.6 不保存一个“整份配置 hash”来决定全部重跑，因为那会让字体变化也重新转写整部电影。项目状态按
`transcribe → translate → mux` 和 `transcribe → translate → tts` 两条依赖链保存输入指纹：视频、
字幕、术语表和产物用完整 SHA-256，设置用键顺序无关的规范化 JSON SHA-256。直接输入变化只使所属
阶段过期，再沿依赖链向下传播；人工修改原文字幕不会反向使转写过期。mtime 只属于旧命令的兼容
复用逻辑，不参与项目状态判断。

稳定声明放在 `project.video-txt.json`，上次成功运行的实际音轨、Whisper 解析结果、产物和阶段记录放在
`.video-txt/state.json`。两者都拒绝 `api_key`、`token` 等秘密字段，只保存凭据环境变量名。

## 局部重新识别为什么分核心区与上下文区

Whisper 如果只听严格的错误区间，边界处经常丢掉半句话；如果把前后上下文也直接拼回 SRT，又会重复
紧邻的旧字幕。v0.7 因此把范围分成两层：核心区决定删除哪些旧 cue 和接纳哪些新 cue，实际提取区在
两侧默认各多 1.5 秒。Whisper 看到完整语境，拼接时只保留中心落在核心区内的新 cue，并把跨边界的
时间裁回核心区；若跨边界文本与紧邻旧 cue 相同，则判为 padding 重复并丢弃。

所有保留的旧 cue 不改文字和起止时间，新 cue 加回“提取区起点”成为全片绝对时间。新 cue 内部若
重叠，只向后收紧后一条的起点；最后按时间排序并连续编号。报告同时保留被删旧 cue 与实际加入的新
cue，既能人工核对，也给 v0.7.1 的局部翻译更新留下确定输入。

## 局部修复之后：只翻译变过的行

`retranscribe-range` 改的是几句话，重译的却是整部片子——一部电影 1683 条字幕，本机模型要跑几十分钟，
而且会把人工改过的译文一起冲掉。`--reuse` 拿修复前的原文字幕做比对，读得一样的行沿用旧译文。

匹配只看文字（折叠空白、忽略大小写），不看序号也不看时间轴：局部重识别之后整份文件都会重新编号，
边界那几条的时间戳也会动，按位置匹配等于什么都匹配不上。同一句话在片中说过多次（"Yes."、
"Come on."）时按开始时间取最近的一条——同样的文字在不同场景里可能有不同的译法，就近的那条更可能是
这一处的。两份旧文件条数对不上时直接报错：这说明它们不是同一次翻译的产物，任何猜测都可能把译文
对错行。

沿用下来的译文同时进入 `translate_cues` 的 `translations` 字典，因此它们也是新译行的上下文，
修复处的语气和术语不会断开。

实测（真实影片里连续 30 条字幕，本机 qwen3.5-35b-a3b）：整段重译 10.8 秒，改 3 条后带 `--reuse`
重跑 3.2 秒，其余 27 条逐字未变。

翻译审计没有做增量：1683 条字幕的完整审计实测 10 毫秒，省下来的时间还不如多写一行代码的风险大，
而且全片审计能查出接缝处新出现的问题。

## 一次真实的多说话人配音：声学分割不可靠时怎么修

素材：`chatgpt_astra_raw.mp4`，2 分 43 秒的 ChatGPT 演示合集，人机对话，背景音乐从头铺到尾，
37 块字幕。目标是人类提问和助手应答各一个音色。

**pyannote 分不对，两种参数都不对。** `--speakers 2` 得到 SPEAKER_01 58% / SPEAKER_00 42%，
但把台词对回去就发现两个标签各自都混着真人和助手：SPEAKER_01 里既有 "Done."、"Opening Blender."
这类应答，也有 "Let's build a presentation…" 这样 12.5 秒的人类长句。放开成
`--min-speakers 2 --max-speakers 8` 得到 4 个说话人，看着像找对了（片子确实是几个不同的人各演一段），
但第 37 块那**一整句助手应答被从中间劈成 SPEAKER_03 + SPEAKER_02 两半**——说明这不是"人多导致的
聚类困难"，是配乐把声学特征污染了。

**台词内容才是可靠依据。** 谁在提要求、谁在应答，读一遍原文一目了然，比任何声学特征都稳。
`speakers.json` 的 `turns` 可以手写，写成一块字幕一条 turn、标签用 `HUMAN` / `ASSISTANT`，
`assign_speakers` 照样按重叠时长把每块归到位。保留 `cache_key` 不动，命令直接复用手改的结果，
不会重跑 pyannote。

**自动切的参考片段被污染了，这是音色难听的直接原因。** `pick_reference_window` 按"同一说话人的
连续块"找 3–12 秒窗口——分割一错，参考片段里就同时有两个角色：

```
SPEAKER_00-clean-64000.txt: Sure. Okay, yeah, this is awesome. I want you to make a 3D game...
SPEAKER_01-clean-53000.txt: Make it fun. Right, I can help with that. Can you go to eBay...
```

两段都是"提问方 + 应答方"的混合体，等于拿同一锅汤克隆了两次，所以两个音色听着差不多、而且都偏慢。
改成从 `dub-cache/bgm/vocals.flac` 干声里手工切纯净片段（助手用 4 段纯应答拼成 8.85 秒，
人类用一段连续提问 7.44 秒），同一条命令前后对比：

| | 污染的参考 | 纯净的参考 | 参照：单音色版 |
| --- | --- | --- | --- |
| 语速 | 7.3 字/秒 | **8.0** | 9.0 |
| 每段填充 | 1.70s | **0.84s** | 1.03s |
| 需压缩改写的句子 | 20 | **9** | 8 |
| 最紧一句需加速 | 2.63× | **1.65×** | 1.22× |
| 加速重念 / 拉伸 | 16 / 10 | **11 / 6** | 4 / 2 |
| 仍超出槽位 | 1 句（0.4 秒） | **0** | 0 |
| 时间轴漂移 | 0.4 秒 | **0.0** | 0.0 |

语速是连锁反应的源头：参考念得慢，合成就慢，塞不进槽位的句子成倍增加，`--fit-duration` 只好把
中文压成"完"、"棒！"这种电报体。**参考片段的质量决定整条配音链的宽裕度**，值得开跑前花两分钟
`cat` 一遍 `reference/*.txt` 确认。

## 局部重识别为什么常常"修不动"

同一段字幕被劈在半句上（`…that it's just` / `slightly damaged…`），`retranscribe-range` 重跑一遍，
**词一个没听错，分块位置和上次一模一样**。原因是问题不在识别，在 Whisper 的解码窗口——同样的音频
同样的模型，切法自然复现。加 `--refine-subtitles` 用词级时间戳重建块边界才真正解决：
130 个词重排成 13 块，最长 5.7 秒，跨句的地方都断在句末。

这跟 `--overwrite` 重跑翻译"重新掷骰子"是两种性质：翻译重跑会掷出**不同的**错误，
局部重识别会复现**同一个**错误。所以修分块错位别指望多跑几次，直接上 `--refine-subtitles`。

修完还要看首块：`--padding`（默认 1.5 秒）会把上一块的尾巴连带重识别一遍，第一块常常重复上一块
结尾的几个词（实测第 22 块结尾 "…a licensing agreement template."，第 23 块又以
"needs a licensing agreement template" 起头），不删就会连着两块说同一句话。

## 人工校订为什么锚定原文而不是字幕编号

一份真实的电影字幕项目里,人工校订最初是一个脚本,里面写死了一个 `{字幕编号: 中文}` 的字典,
34 条,然后原地覆盖 `.srt`。三个问题:编号是唯一锚点且脚本不做任何校验;重新翻译就全丢;
审计看不出哪些行是人工定过的。

编号这一条最危险。同一个项目的 `clean` 就删掉了 8 条幻觉、合并了 3 个零时长尾词——上游任何一次
这样的改动,下面所有编号都平移,34 条修订会**静默地**写到别人的台词上。拿那份 1683 条的英文稿
模拟再删 3 条,验证过一次:

```
原 #110  -> 现 #108   'You can take today to figure out how to'   ← 锚定原文,跟着走
按编号写 #110 -> 'having...'                                       ← 错行,且没有任何提示
```

所以锚点是「原文台词 + 大致说话时间」,和 `reuse.py` 判断「两条 cue 是同一条 cue」用的是同一套:
归一化文本匹配,多个候选时取时间上最近的那个。在那 34 条上实测,原文文本本身就能唯一定位 32 条,
只有 `Oh.` 和 `Oh, good.` 需要时间戳消歧——所以 `at` 设计成可选,有歧义时才强制要求。

时间戳只当 tiebreaker,不做有效性校验。一开始想过「锚点时间和实际位置差太远就报错」,但局部重识别
本来就会让台词挪动一两秒,这会制造大量假警报。改成超过 5 秒只打印提醒:文本匹配才是真锚点,
时间只用来在重复台词之间选一个。

匹配不上必须是硬错误。这是这个格式存在的理由本身——静默失效的校订文件比没有更糟:命令照样返回 0,
而每一条人工定稿都退回成了模型的说法,没有任何地方会提到这件事。

## 术语表起草：用大小写而不是停用词表认专有名词

第一版想过维护一份英语停用词表来排除 `The` / `But` / `Well`，很快发现方向错了：名单永远补不完，
换个片子还要重补。字幕自己已经给了答案——**专有名词会出现在句子中间且仍然大写，并且几乎不会
被写成小写**。两条都要：`I` 满足第一条却不是名字。

所以仍然保留了一份显式名单，但它很短且不随片子变：英语无条件大写的词（`I` / `OK`）、
名字前的头衔（`Mr` / `Dr`）、以及每部片都有的感叹（`God` / `Jesus` / `Christ`）。
阈值是句中大写至少 2 次、小写次数不超过大写次数的 20%。

拿两份真实字幕反复调出来的五个坑，每一个都是先看到噪声、再回去看上下文才找到原因：

1. **句子要跨字幕块拼。** 字幕经常在从句中间断开，按块判断"句首"会把一堆普通词误认成名字。
   做法是把整份字幕接成连续文本再切句。
2. **说话人标签要断句。** Barry S02 的字幕是 `Sasha: Oh my God!` 这种格式，冒号后面的词
   在纯句号断句下全算"句中"——`Oh`(174 次) 和 `Hey`(123 次) 就是这么混进候选的。断句符里
   必须包含 `:`。
3. **`--` 要断句，而且它紧贴前一个词。** `Who is that-- Oh, that's the new guy.` 里
   `--` 前面没有空格，只处理"空格 + 破折号"抓不到。加上这条以后 Barry 的噪声从 6 个降到 4 个，
   命中从 24 涨到 26。
4. **词组必须要求文本上真的相邻，不能只看 token 连续。** `"Oh, Lilypan!"` 的逗号被忽略就成了
   词组 `Oh Lilypan`——既凭空造词条，又把真名字 `Lilypan` 的计数偷走（实测它因此从 3 掉到 2，
   跌破阈值直接消失）。判据是两个词之间除空白外没有别的字符。
5. **所有格、边缘破折号、大小写要归一。** `Blaze's`/`Blaze`、`I--`/`I`、`bonnie`/`Bonnie`
   必须并成一条，否则同名提案多次而且每条计数都偏低。内部连字符要保留，`Jean-Luc` 是一个词。

两份真实数据的最终表现（对照各自手写的术语表）：

| 数据 | 规模 | 候选 | 命中人工表 | 噪声 |
| --- | --- | --- | --- | --- |
| Barry S02 全季（`--min-count 8`） | 4717 cue / 8 集 | 30 | 26/44 | 3（`Oh` `Hey` `Dad`） |
| Toy Story 5（`--min-count 3`） | 1683 cue | 28 | 7/11 | 约 5 |

**漏掉的都是低频名字**：Toy Story 漏的 4 条全片只出现 1–2 次（`Monty` 只说过一次，还是所有格；
`Buzz Lightyear` 整片一句）。频率扫描找不到这些是对的，编出来才是错的。
反方向上它扫出了人工表漏掉的真名字——Toy Story 的 Forky、Dolly、Zerg、Emily，Barry 的 Cleveland，
所以草稿的价值不只是省时间。

同名异写按编辑距离 1 判定（长度 ≥5 才算，短词差一个字母通常是两个词）。这条抓出了 Toy Story
把同一角色拼成 `Jesse`(31 次) 和 `Jessie`(26 次)——人工表只收了后者，`Jesse` 的 31 次全漏在表外。

## 已知现象

- **Whisper 开头容易吐长段落。** 前几句常出现跨越二三十秒的一整段,烧成硬字幕就是一整屏文字。
  v0.3 起可加 `--refine-subtitles`,利用词级时间戳按句末、停顿、时长和两行显示宽度自动重切;
  默认仍保留 Whisper 原始切法,便于旧流程无变化地重跑。
- **`uv sync --extra X` 会卸掉不在列表里的可选依赖。** 它每次都把环境对齐到本次给的列表,不是增量
  安装。单独补装 `--extra diarize` 会把已经装好的 Demucs 和 edge-tts 剪掉,而缺依赖要等命令跑到
  那一步才报出来——分离说话人都跑完了才发现没有 Demucs。装的时候一次写全。
- **pyannote 在 CPU 上跑。** 部分算子在 MPS 上结果不对,有 CUDA 会自动用。长视频第一次要等几分钟。
- **克隆不能并发。** 模型在独立进程里加载一次、一段段合成,`--tts-concurrency` 不起作用。
  每段合成完立刻落盘,中断了重跑只补没做完的。
- **翻译模型返回不合法时**会自动二分拆批重试,原始响应存到 `<字幕名>.debug/` 供事后复盘。
  整次运行成功后这些快照会自动删除(说明失败都被重试挽救了),只有最终失败时才保留;
  显式传 `--debug-dir` 指定的目录不做自动清理。
